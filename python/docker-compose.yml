services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      OPENROUTER_TITLE: agentspace-rag
      CHROMA_PATH: /app/.chroma
    volumes:
      - chroma_data:/app/.chroma
    restart: unless-stopped

volumes:
  chroma_data:
